spring:
  profiles:
    active: dev
  thymeleaf:
    prefix: classpath:templates/
    check-template-location: true
    suffix: .html
    encoding: UTF-8
    mode: HTML5
    cache: false
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

# 日志配置
logging:
  level:
    com.ddl.web.system.generater.mapper: debug
  file:
    max-size: 50M

gen:
  author: drm
  packageName: com.ddl.web.system
  autoRemovePre: true
  tablePrefix: sys_

pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql

#mybatis:
#  # 指定搜索包别名
#  typeAliasesPackage: com.ddl
#  # 配置mapper的扫描 找到所有的mapper映射文件
#  mapperLocations: classpath*:mapper/**/*.xml
#  # 加载全局的配置文件
#  configLocation: classpath:mybatis/mybatis-config.xml
